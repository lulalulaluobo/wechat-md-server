# GitHub Secret Scanning 忽略路径配置
# 说明：source-cache/ 和 data/ 目录下存储的是从微信公众号文章抓取的原始 HTML 缓存文件。
# 这些文件中包含的 wx... 格式字符串是微信平台在文章页面中自动注入的公众号 App ID，
# 属于第三方平台的公开身份标识符，并非本项目的 API 密钥或凭据。
# 这些目录已在 .gitignore 中排除，不会被纳入版本控制。
paths-ignore:
  - "source-cache/**"
  - "data/**"
